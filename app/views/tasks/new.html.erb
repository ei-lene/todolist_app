<h1> Add tasks</h1>
<%= form_for(@task) do |f| %>
  <%= f.label(:description) %>
  <%= f.text_field(:description) %>
  <br>
  <%= f.label("Add to list :") %>
  <%= f.select :list, List.all.collect{|l| [l.name,l.id]} %>
  <br>
  <%= f.submit %>
<% end %>